# revcomps-buy-free-tickets

Create a .env file as in the example one and edit credentials.

Run `npm install` one time, assuming you have node installed.

Run `npx playwright install` and install chrome.

Now you're all set up you can run `npx playwright test` on a schedule.
